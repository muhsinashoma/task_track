http://192.168.32.168/API/get_user_profile_kanban.php?device_user_id=6a3dc9c8-2f0c-45de-9fc9-0d2800691732



--------------------------------------------------------------------------------
https://chatgpt.com/c/68fa0b1d-6e88-8324-b806-fa1547a8e41e



Flutter run key commands.
r Hot reload.
R Hot restart.
h List all available interactive commands.
d Detach (terminate "flutter run" but leave application running).
c Clear the screen
q Quit (terminate the application on the device).

A Dart VM Service on SM A125F is available at: http://127.0.0.1:49285/XptW2_ni3eU=/
The Flutter DevTools debugger and profiler on SM A125F is available at: http://127.0.0.1:9100?uri=http://127.0.0.1:49285/XptW2_ni3eU=/



--------------------------------------------------------------------------------------
https://www.youtube.com/watch?v=vEyEdVY_utg

রিজিক নিয়ে অযথা চিন্তা করবেন না?, মিজানুর রহমান আজহারী, তাং Oct 16, 2025
-----------------------------------------------------------------------------------

Database Value for device_user_id=8086174d-98ef-4f11-ae40-8b9b374ce642

8086174d-98ef-4f11-ae40-8b9b374ce642


Performing hot restart...                                          128ms
Restarted application in 136ms.
Fetching data from: http://192.168.32.168/API/get_task_data_kanban.php
Fetching project list from: http://192.168.32.168/API/get_project_list_kanban.php
✅ Device ID initialized: 8086174d-98ef-4f11-ae40-8b9b374ce642
Projects loaded: 10

----------------------------------------------------------------------------


√  Built build\app\outputs\flutter-apk\app-debug.apk.
Installing build\app\outputs\flutter-apk\app-debug.apk...          29.9s
Syncing files to device SM A125F...                                223ms

Flutter run key commands.
r Hot reload.
R Hot restart.
h List all available interactive commands.
d Detach (terminate "flutter run" but leave application running).
c Clear the screen
q Quit (terminate the application on the device).


--------------------------------------------------------------------------------------
https://chatgpt.com/c/68ef8967-b5c4-8321-abf3-ca9772aebd6c

That file loops:

All projects (tbl_project_details)

	→ All columns (tbl_task_board)

		→ All tasks (tbl_task_name)


-------------------------------------------------------------------------------------------
get_task_data_kanban.php


include 'config.php';

// Get filters
$period         = isset($_GET['period']) ? intval($_GET['period']) : 1;
$unit           = isset($_GET['unit']) ? strtolower($_GET['unit']) : "days";
$projectId      = isset($_GET['project_id']) ? intval($_GET['project_id']) : 0;
$userIdentifier = isset($_GET['user_identifier']) ? $_GET['user_identifier'] : ""; // 👈 new

// Build condition by time
switch ($unit) {
    case "days":  $condition = "created_at >= DATE_SUB(NOW(), INTERVAL $period DAY)"; break;
    case "months": $condition = "created_at >= DATE_SUB(NOW(), INTERVAL $period MONTH)"; break;
    case "years":  $condition = "created_at >= DATE_SUB(NOW(), INTERVAL $period YEAR)"; break;
    case "last days": $condition = "DATE(created_at) = DATE(NOW() - INTERVAL $period DAY)"; break;
    case "last months":
        $condition = "MONTH(created_at) = MONTH(NOW() - INTERVAL $period MONTH)
                      AND YEAR(created_at) = YEAR(NOW() - INTERVAL $period MONTH)";
        break;
    case "last years":
        $condition = "YEAR(created_at) = YEAR(NOW() - INTERVAL $period YEAR)";
        break;
    default:
        $condition = "created_at >= DATE_SUB(NOW(), INTERVAL 1 DAY)";
}

// ---------------- Fetch Boards ----------------
$sql = "SELECT * FROM tbl_task_board WHERE status = 1";
if (!empty($userIdentifier)) {
    $sql .= " AND user_identifier = '$userIdentifier'"; // 👈 filter by user
}
if ($projectId > 0) {
    $sql .= " AND project_id = '$projectId'"; // 👈 filter by project
}

$res = $con->query($sql);

$taskBoards = [];
$columnIds = [];

if ($res && $res->num_rows > 0) {
    while ($row = $res->fetch_assoc()) {
        $row['tasks'] = [];
        $taskBoards[$row['id']] = $row;
        $columnIds[] = $row['id'];
    }
}

$columnIdsStr = $columnIds ? implode(',', $columnIds) : "0";

// ---------------- Fetch Tasks ----------------
$sql = "SELECT * FROM tbl_task_name 
        WHERE column_name IN ($columnIdsStr)
          AND status = 1 
          AND $condition";

if ($projectId > 0) {
    $sql .= " AND project_id = '$projectId'";
}
if (!empty($userIdentifier)) {
    $sql .= " AND user_identifier = '$userIdentifier'"; // 👈 filter by user
}

$sql .= " ORDER BY id DESC";

$tasksRes = $con->query($sql);

if ($tasksRes && $tasksRes->num_rows > 0) {
    while ($task = $tasksRes->fetch_assoc()) {
        $taskBoards[$task['column_name']]['tasks'][] = $task;
    }
}

$taskBoards = array_values($taskBoards);

// Response
header("Access-Control-Allow-Origin: *");
header("Access-Control-Allow-Methods: GET, POST, OPTIONS");
header("Access-Control-Allow-Headers: Origin, Content-Type, Accept, Authorization");
header('Content-Type: application/json');

echo json_encode(['success' => true, 'task_boards' => $taskBoards]);


----------------------------------------------------------------------------------------------

1. meaning of HSVColor


HSVColor is a class in Flutter that represents a color using the HSV color model, which stands for:

H — Hue (the type of color, measured in degrees from 0° to 360°; e.g., 0° = red, 120° = green, 240° = blue)

S — Saturation (how vivid or “pure” the color is; ranges from 0.0 to 1.0; 0 = gray, 1 = fully saturated)

V — Value (brightness; ranges from 0.0 to 1.0; 0 = black, 1 = full brightness)

Why use HSV instead of RGB

RGB (Red, Green, Blue) is great for defining exact colors, but if you want many distinct colors dynamically, HSV is easier because you can:

Vary hue to get completely different colors.

Keep saturation and value consistent so all colors look balanced.

Cycle through hues to generate unique colors for lists of items (like projects) automatically.